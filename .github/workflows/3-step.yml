name: Step 3 # Last step of the exercise

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["update-github-info", "Update GitHub Info"]
    types: [completed]

permissions:
  contents: write
  actions: write
  issues: write
  pull-requests: read

env:
  REVIEW_FILE: ".github/steps/x-review.md"

jobs:
  find_exercise:
    name: Find Exercise Issue
    uses: skills/exercise-toolkit/.github/workflows/find-exercise-issue.yml@v0.9.3

  check_step_work:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    name: Check step work
    runs-on: ubuntu-latest
    needs: [find_exercise]
    env:
      ISSUE_REPOSITORY: ${{ github.repository }}
      ISSUE_NUMBER: ${{ needs.find_exercise.outputs.issue-number }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Get response templates
        uses: actions/checkout@v6
        with:
          repository: skills/exercise-toolkit
          path: exercise-toolkit
          ref: v0.9.3

      - name: Find last comment
        id: find-last-comment
        uses: peter-evans/find-comment@v4
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          comment-author: github-actions[bot]
          direction: last

      - name: Update comment - checking work
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          comment-id: ${{ steps.find-last-comment.outputs.comment-id }}
          file: exercise-toolkit/markdown-templates/step-feedback/checking-work.md
          edit-mode: replace

      - name: Check update-github-info.md was updated
        id: check-notes-request
        continue-on-error: true
        uses: skills/action-keyphrase-checker@v2
        with:
          text-file: .github/workflows/update-github-info.md
          keyphrase: "https://awesome-copilot.github.com/workflows/"
          case-sensitive: false

      - name: Check compiled workflow exists
        id: check-lock-file
        continue-on-error: true
        uses: skills/exercise-toolkit/actions/file-exists@v0.9.3
        with:
          file: .github/workflows/update-github-info.lock.yml

      - name: Check compiled workflow references Copilot secret
        id: check-copilot-secret
        continue-on-error: true
        uses: skills/action-keyphrase-checker@v2
        with:
          text-file: .github/workflows/update-github-info.lock.yml
          keyphrase: COPILOT_GITHUB_TOKEN

      - name: Check workflow allows access to Awesome Copilot
        id: check-awesome-copilot-network
        continue-on-error: true
        run: |
          network_config="$(
            awk '
              /^network:[[:space:]]*/ {
                network = 1
                print
                next
              }
              network && /^[^[:space:]-][^:]*:/ {
                exit
              }
              network
            ' .github/workflows/update-github-info.md |
              tr -d "\"'"
          )"
          printf '%s\n' "$network_config" |
            grep -Eqi '(^|[][[:space:],-])(awesome-copilot\.)?github\.com([][[:space:],]|$)|(^|[][[:space:],-])github([][[:space:],]|$)'

      - name: Find generated PR that updates website content
        id: find-generated-pr
        continue-on-error: true
        run: |
          run_created_at="${{ github.event.workflow_run.run_started_at || '' }}"
          if [ -n "$run_created_at" ]; then
            run_created_epoch="$(date -u -d "$run_created_at" +%s 2>/dev/null || echo 0)"
          else
            run_created_epoch=0
          fi
          while IFS=$'\t' read -r pr_number created_at; do
            created_epoch="$(date -u -d "$created_at" +%s 2>/dev/null || echo 0)"
            if [ "$run_created_epoch" -ne 0 ] && [ "$created_epoch" -lt "$run_created_epoch" ]; then
              continue
            fi
            if gh pr diff "$pr_number" --name-only | grep -q '^site/content/github-info.md$'; then
              echo "pr_number=$pr_number" >> "$GITHUB_OUTPUT"
              exit 0
            fi
          done < <(gh pr list --state open --base main --limit 100 --json number,createdAt --jq '.[] | [.number, .createdAt] | @tsv')
          echo "No open pull request updates site/content/github-info.md"
          exit 1
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check generated PR includes source context
        id: check-pr-sources
        if: steps.find-generated-pr.outcome == 'success'
        continue-on-error: true
        run: |
          {
            gh pr view "${{ steps.find-generated-pr.outputs.pr_number }}" --json body --jq .body
            gh pr diff "${{ steps.find-generated-pr.outputs.pr_number }}"
          } | grep -Eiq 'GitHub Blog|GitHub Changelog|github\.blog|awesome-copilot\.github\.com'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update comment - step results
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          comment-id: ${{ steps.find-last-comment.outputs.comment-id }}
          edit-mode: replace
          file: exercise-toolkit/markdown-templates/step-feedback/step-results-table.md
          vars: |
            step_number: 3
            results_table:
              - description: "Checked that update-github-info.md includes a new updater request"
                passed: ${{ steps.check-notes-request.outcome == 'success' }}
              - description: "Checked for .github/workflows/update-github-info.lock.yml"
                passed: ${{ steps.check-lock-file.outcome == 'success' }}
              - description: "Checked that the compiled workflow references COPILOT_GITHUB_TOKEN"
                passed: ${{ steps.check-copilot-secret.outcome == 'success' }}
              - description: "Checked that the workflow allows network access to the Awesome Copilot source"
                passed: ${{ steps.check-awesome-copilot-network.outcome == 'success' }}
              - description: "Checked that an open generated pull request updates site/content/github-info.md"
                passed: ${{ steps.find-generated-pr.outcome == 'success' }}
              - description: "Checked that the generated pull request includes source context"
                passed: ${{ steps.check-pr-sources.outcome == 'success' }}

      - name: Fail job if not all checks passed
        if: contains(steps.*.outcome, 'failure')
        run: exit 1

  post_review_content:
    name: Post review content
    needs: [find_exercise, check_step_work]
    runs-on: ubuntu-latest
    env:
      ISSUE_REPOSITORY: ${{ github.repository }}
      ISSUE_NUMBER: ${{ needs.find_exercise.outputs.issue-number }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Get response templates
        uses: actions/checkout@v6
        with:
          repository: skills/exercise-toolkit
          path: exercise-toolkit
          ref: v0.9.3

      - name: Create comment - step finished - final review next
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          file: exercise-toolkit/markdown-templates/step-feedback/lesson-review.md

      - name: Create comment - add review content
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          file: ${{ env.REVIEW_FILE }}

      - name: Disable current workflow
        run: gh workflow disable "${{github.workflow}}"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  finish_exercise:
    name: Finish Exercise
    needs: [find_exercise, post_review_content]
    uses: skills/exercise-toolkit/.github/workflows/finish-exercise.yml@v0.9.3
    with:
      issue-url: ${{ needs.find_exercise.outputs.issue-url }}
      exercise-title: "Agentic Workflows That Read the Room"

  disable_generated_workflow:
    name: Disable generated updater workflow
    needs: [find_exercise, finish_exercise]
    runs-on: ubuntu-latest
    env:
      ISSUE_REPOSITORY: ${{ github.repository }}
      ISSUE_NUMBER: ${{ needs.find_exercise.outputs.issue-number }}

    steps:
      - name: Disable update-github-info workflow after issue closure
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          issue_state="$(gh issue view "$ISSUE_NUMBER" --repo "$ISSUE_REPOSITORY" --json state --jq .state)"
          if [ "$issue_state" != "CLOSED" ]; then
            echo "Exercise issue is not closed yet; skipping updater workflow disable."
            exit 0
          fi

          workflow_ids="$(gh workflow list --repo "$ISSUE_REPOSITORY" --all --json id,name,path --jq '.[] | select(
            (.name == "update-github-info") or
            (.name == "Update GitHub Info") or
            ((.path // "") | endswith("/update-github-info.lock.yml")) or
            ((.path // "") | endswith("/update-github-info.yml"))
          ) | .id')"

          if [ -z "$workflow_ids" ]; then
            echo "No generated updater workflow found to disable."
            exit 0
          fi

          while IFS= read -r workflow_id; do
            [ -z "$workflow_id" ] && continue
            gh workflow disable "$workflow_id" --repo "$ISSUE_REPOSITORY"
            echo "Disabled workflow id: $workflow_id"
          done <<< "$workflow_ids"
